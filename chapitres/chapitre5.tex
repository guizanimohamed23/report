\section{Introduction}

\justifying
This sprint is dedicated to attack graph generation. The goal is to transform project artifacts into diagrams that show how data, endpoints and vulnerabilities are connected, so these diagrams highlight potential attack paths in the platform.

\section{Sprint Purpose}

\justifying
The purpose of this sprint is to implement the Dashboard's attack-path and scenario generation features. The generator is an AI-assisted service that produces ephemeral attack scenarios (ordered steps) and a graph object (nodes and edges) for visualization in the Dashboard. Unlike static report diagrams, these graphs are runtime artifacts produced on-demand and returned to the frontend for interactive rendering.

\section{Sprint Backlog}

The backlog for this sprint focuses on the AI-driven scenario and risk-generation flows and the Dashboard UI integration. Table~\ref{tab:sprint-backlog-s3} presents the five user stories covering attack scenario generation, remediation, and interactive visualization:

\begin{table}[H]
  \centering
  \caption{Sprint Backlog}
  \label{tab:sprint-backlog-s3}
  \renewcommand{\arraystretch}{1.35}
  \small
  \setlength{\tabcolsep}{8pt}
  \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{1.6cm}|>{\centering\arraybackslash}p{2.2cm}|>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}p{1.2cm}|}
    \hline
    \textbf{ID} & \textbf{Category} & \textbf{User Story} & \textbf{Priority} \\
    \hline
    US-S3-01 & Scenario Gen & As a User, I can request an attack scenario for a specific vulnerability and receive structured steps and a graph object so that I can investigate exploitation paths. & High \\
    \hline
    US-S3-02 & Remediation & As a User, I can click on an attack scenario step in the interactive graph so that I receive contextualized remediation tips and countermeasures to address that specific vulnerability. & High \\
    \hline
    US-S3-03 & Normalization & As the System, I must validate and normalize AI responses into a safe, consistent graph payload (nodes/edges) so that frontend rendering is robust and secure. & High \\
    \hline
    US-S3-04 & Frontend Integration & As a User, the Dashboard should render the returned graph (React Flow) and allow interaction with attack scenario steps so that I can explore potential attack paths. & Medium \\
    \hline
    US-S3-05 & Operations & As the System, I should handle AI timeouts, malformed responses, and return useful error messages so that failures are observable and debuggable. & Medium \\
    \hline
  \end{tabularx}
\end{table}

\section{Requirements Analysis}

\justifying
The requirements analysis for Sprint 3 addresses the attack scenario generation, interactive graph rendering, and remediation features.

\subsection{Generation Pipeline}

The generator implements a compact, AI-assisted runtime pipeline. Figure~\ref{fig:graph-pipeline} illustrates this pipeline: the Dashboard requests a scenario for a selected vulnerability; the backend loads the vulnerability context, builds a controlled prompt, calls the configured AI gateway, extracts and validates the returned JSON, maps normalized steps into an ephemeral graph (nodes and edges), and returns the graph and ordered steps to the frontend for interactive rendering.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.9\textwidth]{uploads/diagrams/graph pipline.png}
  \captionsetup{skip=4pt}
  \caption{Attack graph runtime pipeline}
  \label{fig:graph-pipeline}
\end{figure}

\subsection{Use Case Diagram}

Figure~\ref{fig:use-case-diagram} illustrates the main actors and their interactions for attack-path generation, showing how users request and interact with generated attack scenarios.

\begin{figure}[H]
  \centering
  \fbox{\parbox{0.85\textwidth}{\centering\includegraphics[width=0.85\textwidth]{uploads/diagrams/sprint3 usecase.drawio.png}}}
  \caption{Use case diagram for attack-path generation}
  \label{fig:use-case-diagram}
\end{figure}

\subsection{Use Case Description}

\indent The main use cases of this sprint are summarized in the following tables.

\begin{table}[H]
  \centering
  \caption{Use case description: Request Attack Scenario}
  \label{tab:request-scenario}
  \renewcommand{\arraystretch}{1.4}
  \small
  \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
    \hline
    \textbf{Name} & Request Attack Scenario \\\hline
    \textbf{ID} & UC-S3-01 \\\hline
    \textbf{Actors} & Authenticated User \\\hline
    \textbf{Description} & User requests an AI-generated attack scenario for a selected vulnerability; the system generates structured steps and a graph, validates them, and returns the results for interactive analysis. \\\hline
    \textbf{Precondition} & User is authenticated, a completed scan with vulnerabilities exists, and the AI service is reachable. \\\hline
    \textbf{Nominal Scenario} & 1. User selects a vulnerability and requests a scenario. 2. System gathers context and calls the AI gateway. 3. System parses, normalizes, and validates the AI response into steps and a graph. 4. System returns the graph and ordered steps to the frontend for rendering. \\\hline
    \textbf{Exception Scenario} & AI timeout/unavailable, malformed response, graph validation failure, or user cancellation — system shows appropriate error/fallback and allows retry. \\\hline
    \textbf{Postcondition} & Ephemeral attack scenario (steps + graph) is available for the user; generation event is logged; scenario is not persisted. \\\hline
    \textbf{Logical Constraints} & On-demand generation, safety validation of AI output, timeout limits, and no persistent storage of generated scenarios. \\\hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Use case description: Select Attack Step and View Remediation}
  \label{tab:select-step-remediation}
  \renewcommand{\arraystretch}{1.4}
  \small
  \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
    \hline
    \textbf{Name} & Select Attack Step and View Remediation \\\hline
    \textbf{ID} & UC-S3-02 \\\hline
    \textbf{Actors} & Authenticated User \\\hline
    \textbf{Description} & User selects a node in the generated attack graph and receives contextual remediation guidance tailored to that attack step and underlying vulnerability. \\\hline
    \textbf{Precondition} & An attack scenario is generated and visible; user is authenticated. \\\hline
    \textbf{Nominal Scenario} & 1. User clicks a graph node. 2. Frontend requests remediation for the selected step. 3. System retrieves or computes remediation and displays it in a panel with actionable steps and references. \\\hline
    \textbf{Exception Scenario} & No remediation found, lookup timeout, or service error — show fallback content or retry option. \\\hline
    \textbf{Postcondition} & Remediation panel is shown with concise, actionable guidance; user can continue exploring the graph. \\\hline
    \textbf{Logical Constraints} & Remediation must be relevant, concise, and retrieved/displayed within a short timeout. \\\hline
  \end{tabularx}
\end{table}

\begin{table}[H]
  \centering
  \caption{Use case description: Render and Interact with Attack Graph}
  \label{tab:render-graph}
  \renewcommand{\arraystretch}{1.4}
  \small
  \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
    \hline
    \textbf{Name} & Render and Interact with Attack Graph \\\hline
    \textbf{ID} & UC-S3-03 \\\hline
    \textbf{Actors} & Authenticated User \\\hline
    \textbf{Description} & Frontend renders the generated graph and allows interactive exploration (zoom, pan, select) with access to remediation details for selected steps. \\\hline
    \textbf{Precondition} & Attack scenario (graph object) is available and user is authenticated. \\\hline
    \textbf{Nominal Scenario} & 1. Frontend receives the graph. 2. React Flow renders nodes/edges. 3. User navigates (zoom/pan) and selects nodes to view details and remediation. \\\hline
    \textbf{Exception Scenario} & Rendering errors, oversized graphs, or invalid structure — show fallback (step list) or error with retry. \\\hline
    \textbf{Postcondition} & Interactive graph displayed; user can explore steps and view remediation. \\\hline
    \textbf{Logical Constraints} & Maintain performance for large graphs, ensure node styling indicates severity, and restrict access to authorized users. \\\hline
  \end{tabularx}
\end{table}

\section{Design}

\justifying
This section presents the design elements for Sprint 3 attack graph generation.

\subsection{Graph Data Model}

The attack graph is a simple, on-demand diagram that helps an analyst understand how an attacker could move through the system. It represents a generated scenario as a sequence of human-readable steps â€” each step has a short title, a clear description of the action, and a note about how serious it is. The diagram shows those steps as boxes connected by arrows to indicate possible flows or next actions. When an analyst clicks a box, the interface shows helpful details and suggested countermeasures for that step.

\subsection{Sequence Diagram (runtime)}

The runtime sequence describes the main interactions involved in on-demand scenario generation. Figure~\ref{fig:attack-graph-sequence} illustrates this sequence diagram, which summarizes the key steps from user request through graph rendering, including AI invocation, response validation, and frontend visualization.

\begin{figure}[H]
  \centering
  \fbox{\parbox{1\textwidth}{\centering\includegraphics[width=1\textwidth]{uploads/diagrams/sprint3 sequence.drawio.png}}}
  \caption{Sequence diagram for runtime scenario generation}
  \label{fig:attack-graph-sequence}
\end{figure}

\section{Implementation}

\justifying
In this sprint, we present the interfaces developed for Sprint 3. The implementation includes the attack graph visualization dashboard where analysts can generate and inspect attack scenarios. Figure~\ref{fig:graph-output} illustrates the application interface for interactive attack graph visualization:

\begin{figure}[H]
  \centering
  \centering
  \includegraphics[width=1\textwidth]{uploads/images/graph_generation.png}
  \caption{Dashboard attack graph}
  \label{fig:graph-output}
\end{figure}


\section{Conclusion}

This sprint establishes the attack-graph generation workflow: extraction from source artifacts, enrichment with vulnerability metadata, and rendering to visual formats. The produced graphs will be used in later chapters to illustrate attack paths and support evaluation.
