\section{Introduction}

\indent\justify This sprint focuses on the authentication layer of the HexStrike platform and on the access control rules that protect the main application modules. The goal of this iteration is to provide a secure entry point to the system, manage user sessions with JWT, and restrict sensitive actions such as scan creation and scan history access to authenticated users only.

\indent In practice, this sprint covers the login and registration flow, the guest session mechanism, protected routes on the frontend, and the backend middleware that validates every secured request.

\subsection{Sprint Purpose}

\indent\justify The purpose of this sprint is to secure the platform's entry points and session management to enable safe use of the scanning and analysis features. Concretely, the sprint delivers a robust authentication system, a guest mode for quick evaluation, and middleware that enforces access control for all sensitive API endpoints.

\section{Sprint Backlog}

\indent The backlog of this sprint contains the following user stories. Table~\ref{tab:sprint-backlog-s1} illustrates the six authentication and session management stories prioritized for this sprint:

\begin{table}[H]
    \centering
    \caption{Sprint Backlog}
    \label{tab:sprint-backlog-s1}
    \renewcommand{\arraystretch}{1.35}
    \small
    \setlength{\tabcolsep}{8pt}
    \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{1.6cm}|>{\centering\arraybackslash}p{2.2cm}|>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}p{1.2cm}|}
        \hline
        \textbf{ID} & \textbf{Category} & \textbf{User Story} & \textbf{Priority} \\
        \hline
        US-S1-01 & Authentication & As a User, I can create an account and log in to the platform so that I can securely access my data and scanning features. & High \\
        \hline
        US-S1-02 & Session & As a Guest User, I can start a temporary guest session without creating a persistent account so that I can quickly evaluate the platform. & Medium \\
        \hline
        US-S1-03 & Access Control & As a User, I can access protected pages such as the scanner and scan history so that I can run scans and review past results. & High \\
        \hline
        US-S1-04 & Session Management & As the System, I can verify and refresh the current session by exposing the authenticated user profile so that sessions remain valid and secure. & High \\
        \hline
        US-S1-05 & Tool Integration & As the System, I must resolve and safely execute HexStrike scanner binaries with timeout enforcement and resource limits so that tool outputs are captured reliably for processing. & High \\
        \hline
        US-S1-06 & AI Foundation & As the System, I must integrate Ollama for local AI inference to enable contextual analysis and intelligent recommendation generation without external API dependencies. & High \\
        \hline
    \end{tabularx}
\end{table}

\section{Requirements Analysis}

\indent\justify The requirements analysis for Sprint 1 covers the authentication and access control mechanisms needed to secure the platform.

\subsection{Use Case Diagram}

\indent The actors involved in this sprint are the visitor, the registered user, and the guest user. The visitor can register and log in, the registered user can access protected modules, and the guest user can only use limited temporary access. Figure~\ref{fig:sprint1-usecase} illustrates these three actors and their respective use cases within the authentication system.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{uploads/diagrams/sprint1usecase.drawio.png}
    \caption{Use case diagram for authentication and access control}
    \label{fig:sprint1-usecase}
\end{figure}

\subsection{Use Case Description}

\indent The main use cases of this sprint are summarized in the following tables.

\begin{table}[H]
    \centering
    \caption{Use case description: User Registration}
    \label{tab:register-usecase}
    \renewcommand{\arraystretch}{1.4}
    \small
    \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
        \hline
        \textbf{Name} & User Registration \\\hline
        \textbf{ID} & UC-S1-01 \\\hline
        \textbf{Actors} & User (Visitor) \\\hline
        \textbf{Description} & This use case allows a visitor to create an account by providing the required personal information and credentials. The system validates the input, stores the account securely, and creates an authenticated session. \\\hline
        \textbf{Precondition} & The visitor is not authenticated and is on the registration page. \\\hline
        \textbf{Nominal Scenario} & 1. Visitor opens the registration page. 2. Visitor enters name, email, and password. 3. System validates the input. 4. System hashes and stores the password. 5. System creates the account and issues a JWT token. 6. System redirects the user to the dashboard. \\\hline
        \textbf{Exception Scenario} & 1. Email already exists: System shows an error. 2. Weak password: System asks for a stronger password. 3. Invalid email: System displays a validation message. 4. Database error: System returns a generic failure message. \\\hline
        \textbf{Postcondition} & The account is created and the visitor becomes an authenticated user. \\\hline
        \textbf{Logical Constraints} & Passwords must satisfy security rules. Emails must be unique. Session tokens must be secure. \\\hline
    \end{tabularx}
\end{table}

\begin{table}[H]
    \centering
    \caption{Use case description: User Login}
    \label{tab:login-usecase}
    \renewcommand{\arraystretch}{1.4}
    \small
    \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
        \hline
        \textbf{Name} & User Login \\\hline
        \textbf{ID} & UC-S1-02 \\\hline
        \textbf{Actors} & Registered User \\\hline
        \textbf{Description} & Registered users authenticate with their credentials; the system validates them and establishes an authenticated session using a signed JWT. \\\hline
        \textbf{Precondition} & User has an existing account and the login endpoint is available. \\\hline
        \textbf{Nominal Scenario} & 1. User submits email and password. 2. System validates credentials and issues a signed JWT. 3. Client stores token and redirects to dashboard. \\\hline
        \textbf{Exception Scenario} & 1. Invalid credentials: display authentication error. 2. Repeated failures: account lockout. 3. Service/database error: show retry or service-unavailable message. \\\hline
        \textbf{Postcondition} & User is authenticated and can access protected features; session token is active. \\\hline
        \textbf{Logical Constraints} & Rate-limiting, secure token storage, token expiration and revocation policies. \\\hline
    \end{tabularx}
\end{table}

\begin{table}[H]
    \centering
    \caption{Use case description: Guest Session Initialization}
    \label{tab:guest-usecase}
    \renewcommand{\arraystretch}{1.4}
    \small
    \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
        \hline
        \textbf{Name} & Guest Session Initialization \\\hline
        \textbf{ID} & UC-S1-03 \\\hline
        \textbf{Actors} & Guest User \\\hline
        \textbf{Description} & A visitor can start a temporary, limited session without registering. The system issues a short-lived guest token with restricted permissions. \\\hline
        \textbf{Precondition} & Visitor is unauthenticated and the guest service is available. \\\hline
        \textbf{Nominal Scenario} & 1. Visitor requests guest access. 2. System issues a temporary guest token with limited permissions and expiry. 3. Client stores token and enters a restricted session. \\\hline
        \textbf{Exception Scenario} & 1. Guest quota reached: show availability message. 2. Token/store error: show retry or service-unavailable notice. \\\hline
        \textbf{Postcondition} & Temporary guest session active with constrained features; no persistent data created. \\\hline
        \textbf{Logical Constraints} & Enforce token expiry, limited permissions, and rate-limiting for guest access. \\\hline
    \end{tabularx}
\end{table}

\section{Design}

\indent \subsection{Class Diagram}

\indent The design of this sprint is based on the separation between frontend and backend responsibilities.

\indent On the backend side, the authentication flow is handled by the Auth controller and service, the user data is stored in the Prisma User model, and the secured routes are protected by the authentication middleware. The server also applies rate limiting to login endpoints to reduce brute-force attempts.

\indent On the frontend side, the AuthContext manages the session state, the Login and Register pages collect credentials, and the ProtectedRoute and PublicOnlyRoute components control access to the application pages. Figure~\ref{fig:sprint3-class-diagram} illustrates the key components and their relationships for both frontend and backend architectures.

% Inline class diagram to avoid float-induced blank space
\begin{center}
    \includegraphics[width=0.28\textwidth]{uploads/diagrams/sprint1classediagram.drawio.png}\\
    \captionsetup{skip=2pt}
    \captionof{figure}{Class diagram for Sprint 1}\label{fig:sprint3-class-diagram}
\end{center}

\subsection{Sequence Diagram}

\indent The authentication process involves multiple interactions between the client application and backend services. Figure~\ref{fig:sprint1-sequence-diagram} illustrates the main authentication flows: visitor registration and login. The diagram shows interactions between the user interface, authentication form, backend services, and database, including credential validation, JWT token generation, and session establishment.

\begin{figure}[H]
    \centering
    \fbox{\parbox{0.85\textwidth}{\centering\includegraphics[width=0.85\textwidth]{uploads/diagrams/sprint 1 sequence.drawio.png}}}
    \caption{Sequence diagram for authentication flows}
    \label{fig:sprint1-sequence-diagram}
\end{figure}

\section{Implementation}

\indent In this sprint, we present the user interfaces developed for Sprint 1. The implementation includes the registration form for new users, the login form for returning users, and the guest access option. The following figures illustrate the application interfaces:

\begin{figure}[H]
    \centering
    \fbox{\parbox{0.85\textwidth}{\centering\includegraphics[width=0.85\textwidth]{uploads/images/sign up.png}}}
    \caption{User registration page}
    \label{fig:register-page}
\end{figure}

\begin{figure}[H]
    \centering
    \fbox{\parbox{0.85\textwidth}{\centering\includegraphics[width=0.85\textwidth]{uploads/images/login.png}}}
    \caption{User login page}
    \label{fig:login-page}
\end{figure}

\section{Conclusion}

\indent This sprint establishes the foundation for secure platform access through a robust authentication and session management layer. With user registration, login, guest access, and access control mechanisms in place, the platform is ready to support the scanning and analysis features implemented in subsequent sprints.
