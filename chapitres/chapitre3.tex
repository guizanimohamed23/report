\section{Introduction}

\justify This sprint focuses on the authentication layer of the HexStrike platform and on the access control rules that protect the main application modules. The goal of this iteration is to provide a secure entry point to the system, manage user sessions with JWT, and restrict sensitive actions such as scan creation and scan history access to authenticated users only.

In practice, this sprint covers the login and registration flow, the guest session mechanism, protected routes on the frontend, and the backend middleware that validates every secured request.

\subsection{Sprint Purpose}

\justify The purpose of this sprint is to secure the platform's entry points and session management to enable safe use of the scanning and analysis features. Concretely, the sprint delivers a robust authentication system, a guest mode for quick evaluation, and middleware that enforces access control for all sensitive API endpoints.

\section{Sprint Backlog}

The backlog of this sprint contains the following user stories. Table~\ref{tab:sprint-backlog-s1} illustrates the six authentication and session management stories prioritized for this sprint:

\begin{table}[H]
    \centering
    \caption{Sprint Backlog}
    \label{tab:sprint-backlog-s1}
    \renewcommand{\arraystretch}{1.35}
    \small
    \setlength{\tabcolsep}{8pt}
    \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{1.6cm}|>{\centering\arraybackslash}p{2.2cm}|>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}p{1.2cm}|}
        \hline
        \textbf{ID} & \textbf{Category} & \textbf{User Story} & \textbf{Priority} \\
        \hline
        US-S1-01 & Authentication & As a User, I can create an account and log in to the platform so that I can securely access my data and scanning features. & High \\
        \hline
        US-S1-02 & Session & As a Guest User, I can start a temporary guest session without creating a persistent account so that I can quickly evaluate the platform. & Medium \\
        \hline
        US-S1-03 & Access Control & As a User, I can access protected pages such as the scanner and scan history so that I can run scans and review past results. & High \\
        \hline
        US-S1-04 & Session Management & As the System, I can verify and refresh the current session by exposing the authenticated user profile so that sessions remain valid and secure. & High \\
        \hline
        US-S1-05 & Tool Integration & As the System, I must resolve and safely execute HexStrike scanner binaries with timeout enforcement and resource limits so that tool outputs are captured reliably for processing. & High \\
        \hline
        US-S1-06 & AI Foundation & As the System, I must integrate Ollama for local AI inference to enable contextual analysis and intelligent recommendation generation without external API dependencies. & High \\
        \hline
    \end{tabularx}
\end{table}

\section{Requirements Analysis}

\justify The requirements analysis for Sprint 1 covers the authentication and access control mechanisms needed to secure the platform.

\subsection{Use Case Diagram}

The actors involved in this sprint are the visitor, the registered user, and the guest user. The visitor can register and log in, the registered user can access protected modules, and the guest user can only use limited temporary access. Figure~\ref{fig:sprint1-usecase} illustrates these three actors and their respective use cases within the authentication system.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{uploads/diagrams/sprint1usecase.drawio.png}
    \caption{Use case diagram for authentication and access control}
    \label{fig:sprint1-usecase}
\end{figure}

\subsection{Use Case Description}

The main use cases of this sprint are summarized in the following tables.

\begin{table}[H]
    \centering
    \caption{Use case description: User Registration}
    \label{tab:register-usecase}
    \renewcommand{\arraystretch}{1.4}
    \small
    \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
        \hline
        \textbf{Name} & User Registration \\\hline
        \textbf{ID} & UC-S1-01 \\\hline
        \textbf{Actors} & User (Visitor) \\\hline
        \textbf{Description} & This use case enables a new visitor to create a user account on the platform by providing personal information and credentials. The system validates the input data, applies security measures such as password hashing, stores the account in the database, and initiates an authenticated session. \\\hline
        \textbf{Precondition} & The visitor is not currently authenticated and is accessing the registration form on the platform. \\\hline
        \textbf{Nominal Scenario} & 1. Visitor navigates to the registration page. 2. Visitor enters full name, email address, and password. 3. System validates the input format (email uniqueness, password strength). 4. System hashes the password using bcrypt or similar. 5. System stores the user record in the database. 6. System generates a JWT token. 7. System establishes an authenticated session and redirects to the dashboard. \\\hline
        \textbf{Exception Scenario} & 1. Email already registered: System displays error and prompts for a different email. 2. Weak password: System rejects and provides feedback on password requirements. 3. Invalid email format: System displays validation error. 4. Database error: System logs the error and returns a generic error message to the user. \\\hline
        \textbf{Postcondition} & A new user account is created in the system, an authenticated session is established, and the visitor is now a registered User with access to protected pages. \\\hline
        \textbf{Logical Constraints} & Password must meet security requirements (length, complexity). Email must be unique in the database. Session tokens must be cryptographically secure. \\\hline
    \end{tabularx}
\end{table}

\begin{table}[H]
    \centering
    \caption{Use case description: User Login}
    \label{tab:login-usecase}
    \renewcommand{\arraystretch}{1.4}
    \small
    \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
        \hline
        \textbf{Name} & User Login \\\hline
        \textbf{ID} & UC-S1-02 \\\hline
        \textbf{Actors} & User (Registered account holder) \\\hline
        \textbf{Description} & This use case allows a registered user to authenticate and gain access to the platform. The user provides credentials, the system validates them against stored records, generates a signed JWT token, and establishes an authenticated session. \\\hline
        \textbf{Precondition} & The user has a registered account in the system and is not currently authenticated. The login endpoint is available and functioning. \\\hline
        \textbf{Nominal Scenario} & 1. User navigates to the login page. 2. User enters registered email and password. 3. System validates the format of provided credentials. 4. System retrieves the user record from the database. 5. System compares the provided password hash with the stored hash. 6. System generates a signed JWT token with user identity and expiration. 7. System stores the token on the client side (localStorage or HttpOnly cookie). 8. System redirects to the dashboard. \\\hline
        \textbf{Exception Scenario} & 1. Email not found: System displays a generic authentication error message (for security). 2. Password incorrect: System displays authentication error and increments failed attempt counter. 3. Account locked: After N failed attempts, system temporarily locks the account and displays a lockout message. 4. Token generation error: System logs the error and displays a retry message. 5. Database unavailable: System displays a service unavailable message. \\\hline
        \textbf{Postcondition} & User is authenticated, a valid JWT session token is active, and the user can access protected pages and features of the platform. \\\hline
        \textbf{Logical Constraints} & Login attempts must be rate-limited to prevent brute-force attacks. JWT tokens must have an expiration time. Failed login attempts must be logged for security auditing. \\\hline
    \end{tabularx}
\end{table}

\begin{table}[H]
    \centering
    \caption{Use case description: Guest Session Initialization}
    \label{tab:guest-usecase}
    \renewcommand{\arraystretch}{1.4}
    \small
    \begin{tabularx}{\textwidth}{|p{2.5cm}|X|}
        \hline
        \textbf{Name} & Guest Session Initialization \\\hline
        \textbf{ID} & UC-S1-03 \\\hline
        \textbf{Actors} & Guest User (Visitor) \\\hline
        \textbf{Description} & This use case enables a visitor to access a limited version of the platform without creating a persistent account. The system generates a temporary session token with restricted permissions, allowing the guest to explore platform features with constraints on data persistence and history access. \\\hline
        \textbf{Precondition} & The visitor is not registered, not authenticated, and is on the login or home page. The guest session service is available. \\\hline
        \textbf{Nominal Scenario} & 1. Visitor clicks ``Guest Access'' or ``Try Platform'' button. 2. System generates a temporary guest token with limited permissions (read-only, no persistence). 3. System sets an expiration time (e.g., 24 hours) on the guest session. 4. System stores the guest token on the client side. 5. System establishes a guest session context. 6. System redirects to the dashboard with guest-specific UI limitations (e.g., disabled scan history, disabled exports). \\\hline
        \textbf{Exception Scenario} & 1. Guest session limit reached: System informs the user that guest sessions are temporarily unavailable and suggests registration. 2. Token generation error: System logs the error and displays a retry message. 3. Session store unavailable: System displays a service unavailable message. \\\hline
        \textbf{Postcondition} & A temporary guest session is established, a guest token is stored on the client, and the user can access limited platform features with a constrained interface. Guest scan history and persistent data storage are disabled. \\\hline
        \textbf{Logical Constraints} & Guest sessions must have a defined expiration period. Guest tokens must have limited permissions (read-only access, no scan creation). Guest sessions must not persist data across sessions. Concurrent guest sessions may be rate-limited per IP address. \\\hline
    \end{tabularx}
\end{table}

\section{Design}

\subsection{Class Diagram}

The design of this sprint is based on the separation between frontend and backend responsibilities.

On the backend side, the authentication flow is handled by the Auth controller and service, the user data is stored in the Prisma User model, and the secured routes are protected by the authentication middleware. The server also applies rate limiting to login endpoints to reduce brute-force attempts.

On the frontend side, the AuthContext manages the session state, the Login and Register pages collect credentials, and the ProtectedRoute and PublicOnlyRoute components control access to the application pages. Figure~\ref{fig:sprint3-class-diagram} illustrates the key components and their relationships for both frontend and backend architectures.

% Inline class diagram to avoid float-induced blank space
\begin{center}
    \includegraphics[width=0.28\textwidth]{uploads/diagrams/sprint1classediagram.drawio.png}\\
    \captionsetup{skip=2pt}
    \captionof{figure}{Class diagram for Sprint 1}\label{fig:sprint3-class-diagram}
\end{center}

\subsection{Sequence Diagram}

The authentication process involves multiple interactions between the client application and backend services. Figure~\ref{fig:sprint1-sequence-diagram} illustrates the main authentication flows: visitor registration and login. The diagram shows interactions between the user interface, authentication form, backend services, and database, including credential validation, JWT token generation, and session establishment.

\begin{figure}[H]
    \centering
    \fbox{\parbox{0.85\textwidth}{\centering\includegraphics[width=0.85\textwidth]{uploads/diagrams/sprint 1 sequence.drawio.png}}}
    \caption{Sequence diagram for authentication flows}
    \label{fig:sprint1-sequence-diagram}
\end{figure}

\section{Implementation}

In this sprint, we present the user interfaces developed for Sprint 1. The implementation includes the registration form for new users, the login form for returning users, and the guest access option. The following figures illustrate the application interfaces:

\begin{figure}[H]
    \centering
    \fbox{\parbox{0.85\textwidth}{\centering\includegraphics[width=0.85\textwidth]{uploads/images/sign up.png}}}
    \caption{User registration page}
    \label{fig:register-page}
\end{figure}

\begin{figure}[H]
    \centering
    \fbox{\parbox{0.85\textwidth}{\centering\includegraphics[width=0.85\textwidth]{uploads/images/login.png}}}
    \caption{User login page}
    \label{fig:login-page}
\end{figure}

\section{Conclusion}

This sprint establishes the foundation for secure platform access through a robust authentication and session management layer. With user registration, login, guest access, and access control mechanisms in place, the platform is ready to support the scanning and analysis features implemented in subsequent sprints.
